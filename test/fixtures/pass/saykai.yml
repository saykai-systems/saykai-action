version: "1.0"
project: "Saykai-Action-SelfTest-Pass"

ci_gate:
  strict: true
  deep_scan: true

scan:
  mode: folder
  root: test/fixtures/pass/scan_targets

outputs:
  directory: outputs

audits:
  directory: audits

policy:
  file: test/fixtures/common/saykai-policy.yml
